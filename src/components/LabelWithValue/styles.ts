import { makeStyles } from "@material-ui/core"

export const useStyles = makeStyles({
  label: {
    color: '#696977',
    fontSize: '2.4vh'
  },
  value: {
    fontWeight: 'bold',
    marginBottom: '5%',
    fontSize: '2.8vh'
  }
})
