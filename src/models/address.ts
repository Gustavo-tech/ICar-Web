export type AddressPt = {
  cep: string;
  localidade: string;
  logradouro: string;
  bairro: string;
}

export type AddressEn = {
  zipCode: string;
  location: string;
  district: string;
  street: string;
}
