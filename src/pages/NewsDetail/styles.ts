import { makeStyles } from "@material-ui/core"

export const useStyles = makeStyles({
  mainContainer: {
    display: 'block',
    margin: '3% auto',
    width: '67%',
  },
  paper: {
    padding: '2%',
    wordWrap: 'break-word'
  }
})
